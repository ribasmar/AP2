#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int main(void){
  int fatorial = 0;
  int parametro = 0;
scanf("%d", &parametro);
 fatorial = parametro;
if ( == fatorial > := fatorialfatorial) {
}
 parametro = parametro;
printf("%d ",fatorial);


return 0;
}
